# Wholesale scaling model

Merged in from `~/kairos-wholesale` on 2026-09-06.

`build_model.py` generates `wholesale_model.xlsx`, a what-if model for wholesale
scaling. Needs `openpyxl`. Standalone: it does not read from the Kairos app or
its Sheets backend.
